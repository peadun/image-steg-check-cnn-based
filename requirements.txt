# Core ML stack
tensorflow>=2.16

# Image handling
pillow>=10.0
numpy>=1.24

# Web server (used by server.py)
flask>=3.0

# Optional — used only by efficientnet_inference.py's TTA inference helpers
# (already covered by numpy + pillow + tensorflow above; no extra deps needed)
